#!/bin/sh

# Pre-commit hook - fast checks only (<10 seconds)
echo "🔍 Running pre-commit checks..."

# Check for circular dependencies (fast, ~2s)
echo "🔄 Checking for circular dependencies..."
pnpm check:circular
if [ $? -ne 0 ]; then
  echo "❌ Circular dependencies detected! Please fix them before committing."
  exit 1
fi

echo "✅ Pre-commit checks passed"
echo "💡 Run 'pnpm test:fast' manually for quick test verification"
